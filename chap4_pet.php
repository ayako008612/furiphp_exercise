<?php
function display_msg($owner){ 
    $msg = <<<EOM
$name さんは
$animal を
$count 匹飼っています。\n
EOM;
    echo $msg;
}  

$animal = array($owners);
$animal_types(array_unique($owners));

$animal = $animal_types;
$animal = implode(',',$animal);

$animal = array($owners);
count($animal);
